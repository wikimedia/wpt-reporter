{
  "name": "wptstatsv",
  "version": "0.0.1",
  "bin": "./lib/index.js",
  "description": "A thin wrapper for the WebPageTest API that sends metrics to statsv",
  "license": "Apache-2.0",

  "repository": {
    "type": "git",
    "url": ""
  },
  "scripts": {
    "test": "mocha && eslint ."
  },
  "engines": {
    "node": ">=0.10.7"
  },
  "devDependencies": {
    "eslint": "1.2.1",
    "mocha": "^2.2.5"
  },
  "main": "./lib/index.js",
  "dependencies": {
    "minimist": "1.1.3",
    "request": "2.61.0",
    "webpagetest": "0.3.3"
  }
}
